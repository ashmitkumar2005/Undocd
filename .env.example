# GitHub PAT — fine-scoped, contents:write on ashmitkumar2005/Undocd
# Required for /api/queue to write _queue.json. Without it, queueing returns 503.
GITHUB_TOKEN=

# Optional overrides (defaults shown)
GITHUB_OWNER=ashmitkumar2005
GITHUB_REPO=Undocd
GITHUB_BRANCH=main
